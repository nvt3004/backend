services:
  stepstothefuture_be:
    container_name: stepstothefuture_be
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - '8080:8080'

volumes:
  file:static/images:/app/static/images:
  postgres-data:
  redis-data:

networks:
  my-network:
    driver: bridge