package com.repositories;

import com.entities.VersionSale;
import org.springframework.data.jpa.repository.JpaRepository;

public interface VersionSaleJPA extends JpaRepository<VersionSale, Integer> {
}
